monitor:
  submission_db: "sqlite:///state/metadata.sqlite"
  interval: 10 # in seconds

buckets:
  inbox:
    inbox-le-1: "buckets/inbox-le-1.config.yaml"
    inbox-le-2: "buckets/inbox-le-2.config.yaml"
  consented: "buckets/consented.yaml"
  nonconsented: "buckets/nonconsented.yaml"
